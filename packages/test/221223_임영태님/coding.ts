/**
 * str 배열안에 중복된 문자열이 있으면 true, 없으면 false를 return 해주세요
 */
export function hasUniqueCharacters(str) {

}

/**
 * 오름차순 정렬을 먼저한 후 str 배열안에 중복된 문자열이 있으면 true, 없으면 false를 return 해주세요
 */
export function hasUniqueCharactersSort(str) {

}

/**
 * linkedList 에서 중복된 node를 삭제해 주세요.
 */
export function removeDuplicates(list) {
/*
    {
        item: ""
        id: ""
    }
 */


}
